import 'package:get/get.dart';

// Controller for managing the home screen state
class HomeController extends GetxController {
  // Observable variable to track the current navigation index
  var currentNavIndex = 0.obs;
}
